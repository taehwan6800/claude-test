# 사용자 지원 지침

- 사용자는 컴퓨터 초보자이므로 전문 용어를 최대한 피하고, 꼭 필요한 용어는 쉬운 말로 설명한다.
- 모든 안내는 사용자가 그대로 따라 할 수 있도록 한 단계씩 차근차근 설명한다.
- 한 번에 너무 많은 작업을 요구하지 말고, 각 단계가 끝났는지 확인하며 다음 단계로 진행한다.
- 사용자가 모르는 것처럼 보이거나 요청의 의도가 불분명하면, 임의로 추측하지 말고 역으로 질문하여 의도를 확인한다.
- 의도를 확인하는 질문은 다음 형식으로 작성한다.
  - 객관식 선택지 4개를 제시한다.
  - 마지막에 자유롭게 직접 입력할 수 있는 주관식 선택지 1개를 제시한다.
  - 각 선택지는 서로 겹치지 않고 이해하기 쉽게 작성한다.
- 사용자가 선택한 답변을 바탕으로 다음 안내를 조정하고, 필요한 경우 같은 방식으로 추가 질문한다.
- 설명은 친절하고 부담 없는 말투로 작성하되, 불필요하게 길게 늘이지 않는다.

# 카페 운영 상시 규칙 (원고·글 등록)

세부 절차는 `cafe-ops` 스킬(.claude/skills/cafe-ops/SKILL.md)을 따른다. 아래는 항상 지켜야 하는 규칙이다.

- **비실명 금지**: '천안아산 동네사람들' 카페에서 별명 목록에 '비실명' 표시가 붙은 별명(현재 새우탕수육)은 글·댓글·좋아요에 절대 쓰지 않는다. '써본언니들'은 모든 별명 사용 가능.
- **등록 시간**: 글 등록은 07:00~23:30에만 한다. 원고 작성은 그 밖의 시간이나 사용자가 부를 때 한다.
- **글마다 하는 일**: 카페별 맨 위 별명부터 차례로 글쓴이, 댓글은 글쓴이가 아닌 별명, 대댓글은 글쓴이가 그 댓글의 "답글쓰기"를 눌러 **답글 전용 칸(.reply-comment-register)**에 작성해 한 칸 안쪽 답글로 들어가게 한다(일반 댓글 칸에 넣으면 안 됨), 좋아요 0~2개(글쓴이·비실명 제외), 조회수 올리기 12~25 무작위, 등록 후 '이동'을 눌러 글목록에서 확인.
- **원고 규칙**: 마침표(.)와 소수점, "..."는 쓰지 않는다. 특정 상호·브랜드명을 쓰지 않는다. 실제 방문·사용 근거가 없는 특정 업체 후기를 꾸미지 않고 질문·정보 공유 형태로 쓴다. 댓글은 글마다 없음 / 댓글 1개 / 댓글 1개+대댓글 1개 세 종류를 약 4:3:3으로 섞는다. 천안아산 '중고거래·나눔' 게시판은 사이트 오류로 글쓰기가 안 되므로 원고를 만들지 않는다.
- **원고 자동 보충**: 대화가 시작되면 먼저 카페별 안 쓴 원고 수를 확인하고, 100개 이하인 카페가 있으면 묻지 않고 20개씩 검수(`scripts/review-batch.js`)하며 100개를 보충한다(저장은 `scripts/save-batch.js`).
- **득템·할인정보 원고**: 매일 KST 7시에 클라우드 예약 작업이 노션 "득템·할인정보" 페이지를 자동으로 확인해 그날 항목 5개로 '써본언니들' 원고를 짓고 저장·push까지 한다(글 등록은 하지 않음). 자세한 내용은 `cafe-ops` 스킬 2-2·2-3 참고.
- **안전**: 로그인은 사용자가 직접 한다(비밀번호를 채팅으로 받지 않는다). 사람 없이 오래 도는 등록 프로그램은 사용자가 직접 켠다. 시스템이 허용하지 않은 작업은 우회하지 않고 멈춰서 이유를 설명한다.